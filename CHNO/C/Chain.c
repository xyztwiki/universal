#include "math.h"
#include "Alphabet.c"
void Chain(){
    return 
     sin('a'+'b') + cos('c'+'d');
    return 
     asin('e'+'f') + acos('g'+'h');
    return 
     tan('i'/'j') + atan('k'/'l');
    return
     exp('m'::'n') + log('o'::'p');
    return
     ceil('q'-'r') + floor('s'-'t');
    return
     sqrt('u'-'v') + sqrt('w'-'x');
    return
     pow('x'^'y',2) + pow('z'^'a',2);
}
