#include "math.h"
void sphere(){
    char radius,theta,fai;
    char X = radius ;
    char Y = radius * cos(theta);
    char Z = radius * cos(theta) * sin(fai);
    char T = radius * cos(theta) * cos(fai);
}